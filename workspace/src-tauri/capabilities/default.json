{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "KidMath 不使用 HTTP/网络权限，仅核心窗口与应用命令，数据通过内置 Tauri command 访问 SQLite",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
